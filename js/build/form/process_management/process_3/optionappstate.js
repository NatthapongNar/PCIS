/**
 * Created by Natthapong on 21/7/2557.
 */
$(function() {

	
	/*
	var objstartDate = $('input[name="statusdate"]').val();
	var objendDate	  = $('input[name="drawdowndate"]').val();
	
	if(objstartDate != "" && objstartDate != '01/01/1900' && objendDate != "" && objendDate != '01/01/1900') {
		
		var StartdateParts = objstartDate.split("/");
	    var SD 	   = new Date(StartdateParts[2], StartdateParts[1] - 1, StartdateParts[0]);
	    
	    var EnddateParts = objendDate.split("/");
	    var ED 	 = new Date(EnddateParts[2], EnddateParts[1] - 1, EnddateParts[0]);
	    
	    var difference  = ED - SD;
	    var days 		= difference / (1000*60*60*24);
	    var day_conv	= Math.floor(days);
	     
	    $("#countday").val(day_conv);
		
	}
	
	$('input#objDrawdown').on('blur', function() {
		var startDate = $('input[name="statusdate"]').val();
		var endDate	  = $('input[name="drawdowndate"]').val();
		
		console.log('cal day start...');
    	var StartdateParts = startDate.split("/");
        var SD 	   = new Date(StartdateParts[2], StartdateParts[1] - 1, StartdateParts[0]);
        
        var EnddateParts = endDate.split("/");
        var ED 	 = new Date(EnddateParts[2], EnddateParts[1] - 1, EnddateParts[0]);
        
        var difference  = ED - SD;
        var days 		= difference / (1000*60*60*24);
        var day_conv	= Math.floor(days);
         
   		$("#countday").attr('value', day_conv).val(day_conv);
	});
	*/
});